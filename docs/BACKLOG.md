# Masal Amca — backlog

Follow-up work tracked for later. Check items off as you complete them.

- [ ] **Son Dinlenenler alignment** — On Ana Sayfa (`HomeView`), the horizontal `StoryCard` row: tiles misalign when a story title wraps to two lines. Fix layout (e.g. fixed title height, line limits, or `VStack` alignment inside `StoryCard`).

- [ ] **Premium subscription review** — Audit trial, paywall, StoreKit flow, and consistency with `SubscriptionManager` / `PaywallView`.

- [ ] **More voice options** — Expand voice picker / preferences; add more voices overall and more **premium** AI voice options (`StoryPreferences`, edge TTS config, voice list UI).

- [ ] **Günün İpucu** — Add multiple tips and **randomly** pick which one to show on Home (currently static `tipCard` in `HomeView`).

- [ ] **Notifications** — Rework `NotificationsInfoSheet` / scheduling UX and behavior (scope TBD when you start).
